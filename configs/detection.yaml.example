paths:
  frame_dir: "/path/to/frames"
  video_dir: "/path/to/videos"
  output: "output"

model:
  weights: "/path/to/best/weights.pt"
  fallback: "yolo11n.pt"
  imgsz: 640

detection:
  confidence: 0.5
  iou_threshold: 0.45
  max_detections: 300

tracking:
  tracker: "/path/to/bytetrack.yaml"
  persist: true

classes:
  kicker: 0
  ball: 1